using UnityEngine;

namespace Capstone.Game.Inventory {
    // Compatibility shell for scenes saved before the old top HUD was removed.
    [DisallowMultipleComponent]
    public sealed class InventoryMenuHudController : MonoBehaviour {
    }
}
